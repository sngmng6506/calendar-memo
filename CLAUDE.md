# CLAUDE.md

Read [`AGENTS.md`](AGENTS.md) before working in this repository.

Current stack:

1. Electron main process in `electron/`
2. Web renderer in `web/`
3. Windows desktop helper source in `tools/daymark-desktop-host/`
4. Project docs in `README.md` and `docs/`

Default verification:

```powershell
npm run check
```
